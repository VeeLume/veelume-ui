{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "desktop",
	"description": "Desktop-only: the auto-updater plus the relaunch that applies it.",
	"platforms": ["windows", "macOS", "linux"],
	"windows": ["main"],
	"permissions": ["updater:default", "process:allow-restart"]
}
